name: analysis_lints_example
description: "Example demonstrating how to use analysis_lints package"

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.47.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  analysis_lints:
    path: ../
